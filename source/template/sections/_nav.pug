nav.nav
    - var items=['Home', 'About us', 'Our Approch', 'Our Work']
        each item in items  
            if item === 'Home'
                a.nav_links.nav_links--active(href='#')=item
            else
                a.nav_links(href='#')=item